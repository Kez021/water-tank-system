# ==========================================================
# LOCAL CONFIG — H2 database + Gmail SMTP
# Gmail: system.water.tank@gmail.com
# App Password is hardcoded below — no env var needed for local
# ==========================================================

# H2 Database (local only)
spring.datasource.url=jdbc:h2:file:./data/tank_db;AUTO_SERVER=TRUE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
spring.h2.console.settings.web-allow-others=true

# Gmail SMTP — system.water.tank@gmail.com
# App Password generated May 2026 (no spaces)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:system.water.tank@gmail.com}
spring.mail.password=${MAIL_PASSWORD:xcnubvwuobbkklyd}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtp.connectiontimeout=15000
spring.mail.properties.mail.smtp.timeout=15000
spring.mail.properties.mail.smtp.writetimeout=15000
spring.mail.properties.mail.from=${MAIL_FROM:system.water.tank@gmail.com}

# Admin credentials
admin.username=${ADMIN_USERNAME:admin}
admin.password=${ADMIN_PASSWORD:tank@admin2026}
